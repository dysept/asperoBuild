# Admin password (default: aspero2024)
ADMIN_PASSWORD=aspero2024

# Session token (change to something random in production)
SESSION_TOKEN=aspero-admin-v1-2024

# Vercel KV — add from Vercel Dashboard → Storage → KV
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Vercel Blob — add from Vercel Dashboard → Storage → Blob
BLOB_READ_WRITE_TOKEN=
